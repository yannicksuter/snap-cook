# USDA FoodData Central

**CC0-1.0 / US public domain.** No obligations, attribution appreciated.

Feeds `nutrition.*` and `count_weight.*` (household measure to grams — the
"1 medium onion" problem).

Downloaded by `manage.py seed_ingredients` (M5); not vendored, because the full
export is large and versioned upstream. See `../manifest.yaml`.
