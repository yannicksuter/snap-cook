# Eval cases

One directory per case:

```
<id>/
├── source.pdf | source.jpg | source.url
└── gold.yaml      # hand-corrected ground truth
```

Empty for now — cases land with the import pipeline in M3.

Do not commit sources you lack the right to redistribute. A case can reference a
URL instead of vendoring the content.
